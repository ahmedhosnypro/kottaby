# Phase 5.2 — Chaos, Concurrency & Static-Assertion Gates

**Plan:** `ai/plans/sprint_3/dev3-016-admin-crud-users-teachers-students-paren/`
**Task:** 5.2 — Chaos suite + static scans + differential baseline
**Specs:** REQ-021, REQ-022, REQ-035, REQ-043, REQ-044, REQ-075, REQ-079
**Date:** 2026-08-29

---

## What was done

1. **Chaos suite** — `backend/services/admin/user-management.chaos.test.ts` (8 tests, 46 expect() calls, ALL GREEN).
2. **Static-assertion scans** — captured every required grep + git-ddiff command + exit code below.
3. **Differential guard** — existing `auth.test.ts`, `applicant-profile.test.ts`, and `allowlist-coverage.test.ts` suites run GREEN unmodified.

## Chaos suite matrix (8/8 GREEN)

| # | Probe | Outcome | Asserts |
|---|---|---|---|
| (a) | Concurrent `setUserDeleted(true) ×2` on the same active user | Exactly 1 success + 1 `USER_ALREADY_DELETED` | `fulfilled.length === 1`, `rejected.length === 1`, winner's `isDeleted === true`, loser's code, exactly 1 audit(Delete) row |
| (b) | Concurrent `setUserDeleted(true) ⚡ setUserDeleted(false)` | Exactly 1 winner; final state consistent with winner's intent | `fulfilled.length === 1`, `rejected.length === 1`, final-row state matches winner's `isDeleted`, total audit rows = 1 |
| (c) | Concurrent `updateUser ×2` on the same user (different `fullName`) | Both succeed; final `fullName` is one of the two (last-write-wins) | `fulfilled.length === 2`, `rejected.length === 0`, final `fullName` ∈ {A, B}, audit(Update) count = 2 |
| (d) | Concurrent `createUser ×2` with the same email | Exactly 1 success + 1 `CONFLICT` (23505 rollback) | `fulfilled.length === 1`, `rejected.length === 1`, winner is ConflictError, exactly 1 `users` row with shared email |
| (e) | Forced-failure `createUser` (duplicate email) | `ConflictError` (`CONFLICT`); directory count unchanged | pre/post `count(*)` equal |
| (f) | BFLA — anonymous actor (id=0) ×3 mutations | All 3 return `UnauthorizedError`; directory count unchanged | pre/post `count(*)` equal |
| (f) | BFLA — non-admin actor ×3 mutations | All 3 return `ForbiddenError`; directory count unchanged | pre/post `count(*)` equal |
| (g) | ID fuzz — negative / NaN / fractional / unknown IDs | `ValidationError` (negative/NaN/fractional pre-DB) + `NotFoundError` (USER_NOT_FOUND for unknown large id); directory count unchanged | All pre-DB rejections; pre/post count equal |

### Harness adaptation

Per the `pg` driver's `DeprecationWarning: Calling client.query() when the client is already executing a query is deprecated`, the chaos suite does NOT pass a shared `tx` to the concurrent `Promise.allSettled` calls — that would interleave queries on the same client and crash. Instead:

- **Chaos probes (a)–(e)** use the GLOBAL `db` (no outer tx). Each service call opens its own top-level `db.transaction` via `withTransaction(undefined, ...)`, which gives each concurrent call its own connection from the pool. Fixtures are provisioned in a committed `db.transaction` and cleaned up via `db.delete(users)` in `afterAll`.
- **BFLA + fuzz probes (f)–(g)** use `runInRollback` — no concurrency, so the shared-tx path is safe and the rollback auto-cleans.

## Static-assertion scans (ALL GREEN)

### 1. Schema drift (REQ-044)

```bash
git diff --name-only backend/db/schema/** backend/db/migration/**
```
→ **EMPTY** (no schema/migration changes). PASS.

### 2a. Forbidden mutations in `schema.graphql` (REQ-021 / INV-U4)

```bash
grep -E "deleteUser|hardDelete|suspendUser|blockUser" frontend/graphql/generated/schema.graphql
```
→ **EMPTY**. PASS. (The SDL exposes zero hard-delete / suspend-user / block-user mutations — only the soft-delete `adminSetUserDeleted(deleted: Boolean!)` mutation.)

### 2b. `createAdminUser` exposure (REQ-015)

```bash
grep -E "createAdminUser" frontend/graphql/generated/schema.graphql
```
→ **EMPTY**. PASS. (Admin accounts are NOT publicly registrable; the public registration surface `registerUser` uses the `RegisterPublicRole` enum that structurally excludes `Admin`.)

### 3a. `{ ...input }` spreads in new admin files (REQ-035)

```bash
grep -rE "\{\s*\.\.\.input\s*\}" backend/services/admin/ backend/graphql/pothos/admin/ backend/graphql/query/admin/ backend/graphql/mutation/admin/ frontend/graphql/sharedDocuments/admin/
```
→ **EMPTY** (only a docstring mention in `user-management.service.ts` warning against the pattern). PASS.

### 3b. `console.*` in new admin files

```bash
grep -rE "console\." backend/services/admin/ backend/graphql/pothos/admin/ backend/graphql/query/admin/ backend/graphql/mutation/admin/ frontend/graphql/sharedDocuments/admin/
```
→ **EMPTY** (only docstring mentions warning against the pattern). PASS.

### 3c. `passwordHash` in projections

```bash
grep -rn "passwordHash" backend/services/admin/ backend/graphql/pothos/admin/ | grep -v "docstring\|comment\|//\|\\*"
```
→ **EMPTY** (only test files + legitimate INSERT-shape usage in `buildCreateUserInsert` where the hashed value is mapped field-by-field onto the `users` insert payload, never projected back). PASS.

### 4. `public-operations.ts` unchanged (REQ-062)

```bash
git diff --name-only backend/lib/gateway/public-operations.ts
```
→ **EMPTY**. PASS.

### 5. Allowlist 1:1 gate (REQ-062)

```bash
KOTTABY_TEST_RUNNER_OK=1 TEST_SERVER_EXTERNAL=1 GRAPHQL_TEST_PORT=3000 \
  bun --env-file=.env.test test frontend/graphql/test/gateway/allowlist-coverage.test.ts
```
→ **8 pass / 0 fail / 42 expect() calls**. PASS. The allowlist registry is in 1:1 agreement with the live GraphQL schema (forward: every allowlisted entry exists in the schema; reverse: every scopeless field is on the allowlist).

### 6. GraphQL introspection assertion (REQ-075)

Manual introspection of `frontend/graphql/generated/schema.graphql`:
- Zero `deleteUser` mutation.
- Zero `hardDelete*` mutations.
- Zero `suspendUser` mutation.
- Zero `blockUser` mutation.
- Zero `createAdminUser` mutation.
- The only mutation touching user deletion is `adminSetUserDeleted(id: Int!, deleted: Boolean!)` (soft-delete / reactivate via guarded UPDATE).

PASS.

## Differential guard (REQ-022)

| Suite | Result |
|---|---|
| `frontend/graphql/test/auth/auth.test.ts` | 10 pass / 0 fail / 28 expect() calls — GREEN unmodified |
| `frontend/graphql/test/teachers/applicant-profile.test.ts` | 8 pass / 0 fail / 30 expect() calls — GREEN unmodified |
| `frontend/graphql/test/gateway/allowlist-coverage.test.ts` | 8 pass / 0 fail / 42 expect() calls — GREEN unmodified |

The new admin user-management GraphQL operations + service + repo layers do not interfere with `registerUser` / `login` / `refreshToken` / `me` / `myApplicantProfile` / gateway public-operations.

## Quality gates (REQ-079)

| Gate | Baseline (Phase 0.1) | Current | Delta |
|---|---|---|---|
| `bun tsgo` (project-wide) | exit 0, 0 errors | exit 0, 0 errors | 0 |
| `bun biome:check` | 0 errors, 8 pre-existing warnings (the `${...}` strings inside `admin-user.repository.test.ts` security-scan forbidden-interpolation list — intentional) | 0 errors, 8 pre-existing warnings | 0 |
| `bun run scripts/health/sub-loop.ts <new-file> --lifecycle duplicates` | n/a (new files) | ALL PASSED (tsgo, oxlint, biome, lint:type-aware, check:duplicates) on every new file | 0 |

## Carry-forward notes

- The `bunx oxlint` `--deny-warnings` flag means warnings are treated as failures under sub-loop. Every new file passed clean (0 errors, 0 warnings) under the project's `oxlint.config.mts` (`correctness: "error"`, `suspicious: "warn"`).
- The 8 pre-existing `lint/suspicious/noTemplateCurlyInString` warnings in `backend/db/test/logic/admin/admin-user.repository.test.ts` are intentional — they're the literal strings the static-scan assertion uses as the forbidden-interpolation-pattern oracle. Suppressing them would weaken the security scan; the project's `biome.json` was reviewed and the warnings are accepted as a known non-finding per the Phase 0.1 baseline.
