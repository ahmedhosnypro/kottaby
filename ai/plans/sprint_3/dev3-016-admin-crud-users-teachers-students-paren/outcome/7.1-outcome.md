# Phase 7.1 — Canonical Documentation Outcome

**Plan:** `ai/plans/sprint_3/dev3-016-admin-crud-users-teachers-students-paren/`
**Task:** 7.1 — Canonical documentation (`docs/admin/user-management.md`)
**Specs:** REQ-080, REQ-081
**Date:** 2026-08-29

---

## What was done

Created `docs/admin/user-management.md` (212 lines) following the standard structure: Why → Pattern → Rules → What NOT to Do → Rollout Summary → Related Documents.

### Structure-section completeness

| Section | Content | REQ/decision ref |
|---|---|---|
| §1 Why | Workflow 05 identity-core + audit-write half; binds to A.5, A.7, B.6, B.7, INV-U1..U5, INV-TV1, Workflow 05 §1 + §7 | REQ-081 |
| §2.1 Pattern — Directory/Filter/Search Contract | Single-query LEFT JOINs + scalar subselects; page-based pagination (D8 — keyset as documented future refinement); `escapeLikeWildcards` mandate for any future admin search (single canonical escape point at the service + single binding point at the repo's `ilike`) | REQ-009/011/034/046 |
| §2.2 Pattern — Guarded Soft-Delete/Reactivate | Single conditional `UPDATE … WHERE id AND <null-safe inverse-state guard> RETURNING *` (D3); NULL-safe guards — delete: `is_deleted=false OR is_deleted IS NULL`; reactivate: `is_deleted=true` (D4); cold-path probe via `existsById` (columnless, never re-reads sensitive columns); self-protection rule (`id === actorId` → `USER_SELF_DEACTIVATION_FORBIDDEN` BEFORE any write) | REQ-017/018/019, D3, D4 |
| §2.3 Pattern — Role-Child Projection Rules | ONE `users` directory; shared-PK children (`applicants.id = teacher.id = students.id = parents.id = users.id`); LEFT JOINs preserve every `users` row; scalar subselects prevent 1:M fan-out (D2) | REQ-010/013, D2 |
| §2.4 Pattern — Audit-Emission Contract | Writer-side; in-tx via `AuditService.createAuditLog(contract, tx)` (D7); composition-only rule (`AuditLogWriteContract`); append-only (trigger-protected); truncation safety ≤2000 chars at TWO layers (REQ-052); denials write ZERO audit rows (JR-C-1) | REQ-020, REQ-052, D7, JR-C-1 |
| §2.5 Pattern — Shared-PK "One User, Four Role Children" Model + Idempotency Ruling | Shared `users.id` PK across all four role-child tables; idempotency ruling — admin ops NOT in the mandated key set (`docs/IDEMPOTENCY.md`); natural unique-key protection (`users.email` index → 23505 → ConflictError) + guarded state updates; in-flight UI disable mitigation (mirrors DEV1-005 REQ-043 ruling); keyset pagination as documented future refinement (D8) | REQ-040, REQ-043, D8 |
| §3 Rules | 14 enumerated rules — REQ-017/018/019/020/021/030/032/035/040/014/015/052/060/062/070/074/079 | REQ traceability matrix |
| §4 What NOT to Do | 14 anti-patterns — (a) no second LIKE/ILIKE sanitizer; (b) no SELECT-then-UPDATE pattern; (c) no naive `= <bool>` predicate (three-valued SQL logic); (d) **DO NOT copy-paste the `USER_NOT_FOUND` oracle ruling to non-admin surfaces** (D11 explicit warning — the applicant surface inverts this to `null` no-oracle per `docs/teachers/applicant-lifecycle.md` §3); (e) no `teacher` row minted on admin create-teacher (INV-TV1 — B.7); (f) no `createAdminUser` GraphQL exposure (D6 double block); (g) no `{ ...input }` spreads (BOPLA); (h) no audit rows outside caller's tx (D7); (i) no audit rows on denials (JR-C-1); (j) no contact-PII / credentials / tokens in `audit_logs.details`; (k) no touching the registration write path; (l) no re-implementing trial-grant logic (REQ-014 conditional path rule — DEV1-004 deferred); (m) no touching `public-operations.ts` (REQ-062); (n) no renumbering spec-decision files (`docs/specs/state-machine-invariants.md`, `docs/specs/open-decisions-and-gaps.md`) | D3, D4, D6, D7, D11, INV-TV1, JR-C-1, REQ-014/015/021/035/062 |
| §5 Rollout Summary | Lists every shipped file (types, repo, service, audit service, with-transaction helper, escape utility, GraphQL objects + inputs, queries + mutations, frontend documents, frontend containers, app-router pages) + test inventory (147 tests / ~900 expect() calls across 6 files — all GREEN) + quality gates (tsgo exit 0, biome 0 errors, sub-loop exit 0) + schema/migration drift empty | REQ-070/079/044 |
| §6 Scope Split Record | Restated verbatim-style: identity CRUD + audit-write → DEV3-016; cold-start certification → DEV3-018; suspend/block windows → DEV3-017; direct student onboarding → DEV3-019; audit-trail browsing UI → DEV3-020; session governance → DEV3-021; financial auditing → DEV3-022b; plan CRUD → DEV1-005 (separate sprint). Consumer obligations for DEV3-017/018/019/020/021/022b: import-by-reference (never fork — `AuditService.createAuditLog`, `escapeLikeWildcards`, `withTransaction`, `UserRepository.findById`, `AdminUserRepository.*`); honor the in-tx audit-emission contract; honor `actorId = ctx.user.id`; honor `passwordHash` never-touch discipline; honor the `USER_NOT_FOUND` oracle ruling scope (admin-surface-only); honor the role-child projection rules (single `users` directory + shared-PK LEFT JOIN + scalar-subselect discipline) | REQ-080 |
| §7 Related Documents | 14 cross-references — `docs/specs/functional-requirements.md`, `docs/specs/state-machine-invariants.md`, `docs/specs/open-decisions-and-gaps.md`, `docs/workflows/05-admin-governance-override.md`, `docs/graphql/domain-error-extensions-code.md`, `docs/graphql/error-handling-contract.md`, `docs/graphql/api-gateway-and-routing.md`, `docs/auth/jwt-authentication-service.md`, `docs/teachers/applicant-lifecycle.md`, `docs/IDEMPOTENCY.md`, `docs/drizzle/prepared-statements.md`, `docs/drizzle/neon-http-client.md`, `docs/graphql/dataloader-batching.md`, `docs/app/with-page-auth.md`, `docs/backend/cross-stream-contracts.md`, `docs/backend/types-consolidation.md`. NO renumbering of spec-decision files; references are addendum-free | REQ-081 |

## 7.1.SR Semantic Review

- Structure-section completeness: ✅ all six sections present (Why, Pattern, Rules, What NOT to Do, Rollout Summary, Related Documents).
- Every claim traceable to an REQ or decision ref:
  - REQ-017/018/019 (soft-delete/reactivate/self-protection) → §2.2, §3, §4.
  - REQ-020 (audit emission) → §2.4, §3.
  - REQ-021 (governance visibility read-only here) → §3.
  - REQ-030/032 (BFLA / BOLA oracle) → §3, §4 (the D11 explicit warning).
  - REQ-014/015 (creation + admin-role block) → §2.5, §3, §4 (INV-TV1, D6).
  - REQ-035 (cross-role containment) → §3, §4.
  - REQ-040 (atomicity) → §2.4, §3.
  - REQ-052 (audit + log hygiene) → §2.4, §3.
  - REQ-060 (GraphQL surface) → §3.
  - REQ-062 (authScope + public-operations untouched) → §3, §4.
  - REQ-070 (coverage) → §5.
  - REQ-074 (mutation matrix + fixture immutability) → §3.
  - REQ-079 (baseline + quality gates) → §3, §5.
  - D3 (guarded UPDATE) → §2.2, §4.
  - D4 (NULL-safe state guards) → §2.2, §4.
  - D7 (in-tx audit) → §2.4, §3, §4.
  - D8 (page-based pagination; keyset as future) → §2.1, §2.5.
  - D10 (`$all` conjunction) → §3.
  - D11 (`USER_NOT_FOUND` oracle — admin-surface-only; MUST NOT be copy-pasted) → §3, §4.
  - D6 (admin-role double block) → §3, §4.
  - D2 (one directory + scalar subselects) → §2.3.
  - A.5 (audit_logs) → §1, §2.4, §3.
  - A.7 (governance on users) → §1, §3.
  - B.6/B.7 (applicants home; teacher post-verification) → §1, §3.
  - INV-U1..U5 (student/any-user lifecycle) → §1, §3, §4.
  - INV-TV1 (no certification shortcut) → §1, §3, §4.
  - Workflow 05 (§1 Full CRUD Visibility + Control, §5.1 Soft Delete Rules, §7 Audit Trail) → §1, §6.
- NO renumbering of `docs/specs/state-machine-invariants.md` or `docs/specs/open-decisions-and-gaps.md` — references are addendum-free, citation-only.

## Files touched

- Created: `docs/admin/user-management.md` (212 lines).

## Sign-off

**Status:** ✅ PASS — REQ-080 / REQ-081 satisfied. Semantic review complete; every claim traceable to an REQ or decision ref. Ready for Phase 7.2 (AGENTS.md propagation).
