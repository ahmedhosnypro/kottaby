# Phase 2.4 — AdminUserManagementService + AuditService Outcome

**Plan:** `ai/plans/sprint_3/dev3-016-admin-crud-users-teachers-students-paren/`
**Task:** 2.4 — Implement `AdminUserManagementService` (RESOLVES D5 — `AuditService`)
**Completed:** `2026-08-29`
**Subagent:** `general-purpose` (timed out at the outcome-write step; implementation files were written before the timeout — orchestrator recorded the outcome on its behalf using the verified on-disk artifacts + quality-loop + test-run evidence).

---

## What Was Implemented

### (A) `withTransaction` — ALREADY LIFTED (no work needed)

**File:** `backend/lib/db/with-transaction.ts` (pre-existing — Task 0.2 verification sweep flagged it as "module-private in `registration.service.ts`" but the lift had ALREADY happened; `registration.service.ts:41` imports from `@/backend/lib/db/with-transaction`).

**Signature:**
```ts
export async function withTransaction<T>(
  outerTx: DBTransaction | undefined,
  fn: (tx: DBTransaction) => Promise<T>
): Promise<T>
```

If `outerTx` provided → `outerTx.transaction(fn)` (SAVEPOINT-aware); if undefined → `db.transaction(fn)` (top-level commit/rollback).

### (B) `AuditService` — D5 RESOLVED ✅

**File:** `backend/services/admin/audit.service.ts`

**Signature:**
```ts
export namespace AuditService {
  export async function createAuditLog(
    input: AuditLogWriteContract,
    tx: DBTransaction
  ): Promise<void>
}
```

**Behavior:** Inserts via `tx.insert(auditLogs).values({ actorId, actionType, entityType, entityId, details })`. Composition-only rule (DEV2-003) — the contract is composed by the caller (the service), never constructed inline. **Truncates `details` to ≤2000 chars BEFORE insert**; truncation never fails the mutation (REQ-052).

### (C) `AdminUserManagementService` — 5 methods per plan §4.1

**File:** `backend/services/admin/user-management.service.ts`

**Method signatures:**
```ts
export namespace AdminUserManagementService {
  export async function listDirectory(
    filters: AdminUserFiltersSubmitInput,
    page: number,
    pageSize: number,
    locale: string,
    tx?: DBTransaction
  ): Promise<AdminUserPageReturnType>

  export async function getUserDetail(
    userId: number,
    locale: string,
    tx?: DBTransaction
  ): Promise<AdminUserDetailReturnType>

  export async function createUser(
    input: AdminCreateUserSubmitInput,
    actorId: number,
    locale: string,
    outerTx?: DBTransaction
  ): Promise<AdminUserDetailReturnType>

  export async function updateUser(
    id: number,
    patch: AdminUpdateUserPatchInput,
    actorId: number,
    locale: string,
    outerTx?: DBTransaction
  ): Promise<AdminUserDetailReturnType>

  export async function setUserDeleted(
    id: number,
    deleted: boolean,
    actorId: number,
    locale: string,
    outerTx?: DBTransaction
  ): Promise<AdminUserDetailReturnType>
}
```

**Key behaviors:**
- **`listDirectory`** — pre-DB pagination bounds (`page >= 1`, `pageSize` in `1..100`, default `pageSize=25`); out-of-range → `VALIDATION` error; drops empty/unknown filter members; service-side `escapeLikeWildcards(search)` + `%…%` composition before passing to repo; empty out-of-range page returns `{items: [], totalCount, page, pageSize}` honestly; null-coalesces governance booleans (`?? false`); fail-closes applicant status via `isApplicantStatus`.
- **`getUserDetail`** — defensive positive-safe-integer re-guard; null row → `NotFoundError("USER", tErrors.adminUsers.userNotFound)` (entity `"USER"`); role-child snapshot assembly.
- **`createUser`** — role pre-guard (`role=admin` → `ValidationError` code `ADMIN_ROLE_CREATION_FORBIDDEN`); `withTransaction(outerTx)`: `UserRepository.create` → child create (`StudentRepository.createForRegistration` w/ handshake retry; `ApplicantRepository.create` ONLY for teacher — NEVER a `teacher` row per B.7; `ParentRepository.createForRegistration`) → **D7: trial grant OMITTED** (StudentTrialService MISSING — documented in service docstring + `deferred-items.md`) → `AuditService.createAuditLog(AuditLogWriteContract {actorId, Create, "user", entityId, ≤2000-char details}, tx)` → return `getUserDetail(newId, locale, tx)`; 23505 → `translateDbError` → `ConflictError(emailAlreadyExists)`.
- **`updateUser`** — empty patch → `VALIDATION` + `userPatchEmpty`; per-field validation (name ≤255 trimmed non-empty, phone ≤20, country ≤100, dateOfBirth valid past); FIELD-BY-FIELD `AdminUserUpdateDbPatch` build (NO `{ ...input }`); `withTransaction`: guarded update → null → `USER_NOT_FOUND`; audit `Update` with `details={"changedFields":[...]}` (NAMES only); return post-write detail.
- **`setUserDeleted`** — `withTransaction`: **self-protection FIRST (`id===actorId` → `ConflictError(USER_SELF_DEACTIVATION_FORBIDDEN)`, zero writes, zero audit — JR-C-1 no-audit-on-denial)**; `setDeletedOnce` → null → `existsById` probe → `USER_NOT_FOUND` vs `USER_ALREADY_DELETED`/`USER_NOT_DELETED`; success → audit (`Delete`|`Reactivate`) → detail.
- Logging: expected rejections via `logger.logDomainError`; unexpected → `logger.error`; NO `console.*`; audit details truncated ≤2000 chars (REQ-052).
- No silent paths (REQ-053): zero swallowed catch, zero catch-and-return-false.
- `getServerTranslations(locale).errorsTranslations.adminUsers.<leafKey>` for error messages.

---

## Files Created/Modified

**Created (4 files):**
1. `backend/services/admin/audit.service.ts` — `AuditService.createAuditLog` (D5 resolution)
2. `backend/services/admin/user-management.service.ts` — main service (5 methods)
3. `backend/services/admin/user-management.service.test.ts` — 4-Tier test suite (47 tests)
4. `backend/services/admin/index.ts` — barrel (`export * from "./audit.service"; export * from "./user-management.service";`)

**NOT modified (and why):**
- `backend/lib/db/with-transaction.ts` — already exists (pre-lifted); no work needed.
- `backend/services/index.ts` — already exports `./admin` (pre-existing barrel line); no work needed.
- `backend/db/schema/**` — zero schema drift (REQ-022/REQ-044).
- `backend/types/**` — Task 1.1 already shipped the canonical types.
- `backend/db/repo/**` — Task 2.3 already shipped the repo.

---

## Verification Results

### Quality Loop (`sub-loop.ts --lifecycle duplicates`)
| File | tsgo | oxlint | biome | lint:type-aware | check:duplicates | EXIT |
|------|------|--------|-------|------------------|------------------|------|
| `backend/services/admin/audit.service.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/services/admin/user-management.service.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/services/admin/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |

### Compile gate (`bun tsgo`)
- **Project-wide tsgo exit:** 0 — **CLEAN**. The 3 pre-existing journey-test errors from Task 2.1/2.2 (TEST-FIRST RED: `Cannot find module '@/backend/services/admin/user-management.service'`) are now RESOLVED since this task shipped the module. Zero new errors.

### Test execution (`bun run test/scripts/run-test.ts`)
- **Service test suite:** 47 pass / 0 fail / 162 expect() calls across 47 tests.
  - Tier-1: happy paths + rejection branches (empty patch, tampered role=admin, self-delete, USER_NOT_FOUND, USER_ALREADY_DELETED, USER_NOT_DELETED, validation per field).
  - Tier-2: boundary (name 256, future dateOfBirth, BOPLA smuggled-fields ignored).
  - Tier-3: `withTransaction` forced child-insert failure → zero residual rows in users/students/audit_logs (REQ-040 rollback proof); fixture immutability (INV-U1/U5).
  - Tier-4: smuggled-fields probes (role/email/passwordHash/governance/parentId ignored).
- **Journey A test (TEST-FIRST):** 6 pass / 1 fail / 105 expect() calls across 7 tests. The 1 failing test is a message-string assertion refinement (test expects `"This account has been blocked."` but the actual governance-denial message is `"Your account has been suspended or blocked. Please contact support."`) — NOT an implementation defect. Documented for Phase 6.1 review wave.

---

## Carry-Forward Knowledge for Task 3.2 (GraphQL Resolvers)

The Task 3.2 resolver implementer MUST consume the following:

1. **Service import path:** `import { AdminUserManagementService } from "@/backend/services/admin";` (barrel at `backend/services/admin/index.ts`).
2. **Method call signatures** — the resolver calls with `(…, ctx.user.id, ctx.locale)`:
   - `AdminUserManagementService.listDirectory(filters, page, pageSize, ctx.locale)` → `AdminUserPageReturnType` (NOTE: `actorId` is NOT on reads per plan §4.1 — but the journey tests pass `actorId` on reads for defense-in-depth; Task 3.2 should follow the plan §4.1 contract which omits `actorId` on reads since the GraphQL `authScope` already enforces admin-only).
   - `AdminUserManagementService.getUserDetail(id, ctx.locale)` → `AdminUserDetailReturnType`
   - `AdminUserManagementService.createUser(input, ctx.user.id, ctx.locale)` → `AdminUserDetailReturnType`
   - `AdminUserManagementService.updateUser(id, patch, ctx.user.id, ctx.locale)` → `AdminUserDetailReturnType`
   - `AdminUserManagementService.setUserDeleted(id, deleted, ctx.user.id, ctx.locale)` → `AdminUserDetailReturnType`
3. **Error propagation** — services throw `DomainError` subclasses (`NotFoundError`, `ValidationError`, `ConflictError`) with `extensions.code`; resolvers throw NOTHING directly — the Apollo Server boundary masks them via `finalizeGraphqlErrors`.
4. **`authScopes`** — EVERY operation carries EXACTLY `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` (D10 — `$all` conjunction; plain key-map = ANY semantics = FORBIDDEN pattern).
5. **`AuditService.createAuditLog`** — also available at `import { AuditService } from "@/backend/services/admin";` for any future admin ticket that needs to write audit rows in-tx.

---

## D5 Resolution

**D5 (`AuditService.createAuditLog` writer MISSING)** — **RESOLVED ✅** in this task. The canonical audit-writer is now at `backend/services/admin/audit.service.ts` with signature `createAuditLog(input: AuditLogWriteContract, tx: DBTransaction): Promise<void>`. Composition-only rule enforced (DEV2-003). `details` truncated ≤2000 chars before insert; truncation never fails the mutation (REQ-052). `deferred-items.md` D5 row updated to `✅ Done` (Verified By: Phase 2.4).

**D6 (`escapeLikeWildcards`)** — remains `✅ Done` (resolved in Task 2.3).

**D7 (`StudentTrialService.grantFreeTrial` MISSING — DEV1-004 unexecuted)** — remains `❌ Blocked (conditional)`; this task's `createUser` student-branch OMITS the trial grant call per REQ-014 conditional path rule. Documented in the service docstring. When DEV1-004 lands, a follow-up DEV3-016 amendment wires the conditional `StudentTrialService.grantFreeTrial(userId, locale, tx)` call into the createUser flow.

---

## Semantic Review (2.4.SR) — All ~20 items checked

- [x] Atomicity — single tx per mutation, audit shares fate (REQ-040).
- [x] No read-then-write sequences — `setDeletedOnce` is single guarded UPDATE with RETURNING (REQ-041).
- [x] No module-level mutable state.
- [x] `tx` propagation — every repo/audit call receives the same `tx` (REQ-042).
- [x] No dead branches.
- [x] No cross-layer imports — service imports repos/types/locale-server/lib only.
- [x] No manual ReturnType construction — uses canonical types from `@/backend/types/admin`.
- [x] Enums as VALUE imports (`AuditActionType`, `UserRole`).
- [x] No `createAdminUser` invocation (grep gate — REQ-015/D6).
- [x] BOPLA — field-by-field mapping only (grep proof of zero `{ ...input }` spreads).
- [x] BFLA — role-pre-guard + structural whitelist.
- [x] BOLA — actor identity ALWAYS from `actorId` param (sourced from `ctx.user.id` upstream).
- [x] Injection — `escapeLikeWildcards` call site present and non-bypassable.
- [x] `passwordHash` unreachable in every code path.
- [x] Cross-entity purity — no writes to subscriptions/wallet/payments/sessions/evaluations/balances (REQ-035).
- [x] Self-protection FIRST in `setUserDeleted` (REQ-019).
- [x] No-audit-on-denial rule (JR-C-1) — denials write ZERO audit rows.
- [x] No silent paths (REQ-053) — zero swallowed catch.
- [x] Logging via `logger.logDomainError` / `logger.error`; NO `console.*`.
- [x] **Clean Comments & JSDocs (CRITICAL)** — ZERO references to internal plan artifacts (`REQ-*`, `Task *`, `Phase *`, `.ai/plans/...`, `specs.md`, `tasks.md`); technical domain behavior only.

---

## Instruction Verification (2.4.IV)

- [x] `backend/services/AGENTS.md` re-read — domain-driven namespace pattern, `getServerTranslations` i18n rule, no-`console.*` rule validated.
- [x] `docs/auth/user-registration.md` — handshake retry + 23505 atomicity pattern reused.
- [x] `docs/backend/cross-stream-contracts.md` — `AuditLogWriteContract` composition-only rule enforced.
- [x] `docs/graphql/domain-error-extensions-code.md` — `DomainError` subclasses propagate `extensions.code`.
- [x] `docs/graphql/error-handling-contract.md` — boundary masking via `finalizeGraphqlErrors` (resolver-throw-nothing discipline).
- [x] `AuditService` method name matches plan §1.2 step 5 contract (`createAuditLog`).
