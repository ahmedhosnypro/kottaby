# Phase 7.2 — AGENTS.md Propagation Outcome

**Plan:** `ai/plans/sprint_3/dev3-016-admin-crud-users-teachers-students-paren/`
**Task:** 7.2 — AGENTS.md propagation (root + backend/services)
**Specs:** REQ-082
**Date:** 2026-08-29

---

## What was done

Added rule-only one-liners (pointers to `docs/admin/user-management.md`; NO code, NO recipes) to two `AGENTS.md` files:

### 1. `backend/services/AGENTS.md`

Added two new bullet entries immediately after the existing "Teacher applicant lifecycle" pointer (which is the precedent for this format):

```text
- **Admin user-management: identity-and-governance CRUD (directory / detail /
  create / patch / soft-delete / reactivate) lives in
  `AdminUserManagementService` (`backend/services/admin/`) — see
  `docs/admin/user-management.md` for the directory/filter/search contract
  (incl. the `escapeLikeWildcards` mandate), the guarded soft-delete pattern,
  the role-child projection rules, the `USER_NOT_FOUND` oracle ruling
  (admin-surface-only — MUST NOT be copy-pasted to non-admin surfaces), and
  the audit-emission rule (writer-side; in-tx via
  `AuditService.createAuditLog(contract, tx)`; denials write ZERO audit rows —
  JR-C-1).**

- **Audit-emission rule: every admin mutation that commits MUST append exactly
  ONE `audit_logs` row INSIDE the same `withTransaction(outerTx, …)` block via
  `AuditService.createAuditLog(contract, tx)` (composition-only — the contract
  is composed by the calling service, never by the writer). `actorId =
  ctx.user.id` (never input); `entityType` is the short lowercase label;
  `details` is a capped (≤2000 chars) JSON string carrying field NAMES +
  metadata only — NEVER contact-PII, NEVER credentials, NEVER `passwordHash`,
  NEVER email pre/post pairs. Denial paths (anonymous → `UNAUTHORIZED`;
  non-admin → `FORBIDDEN`; self-deactivation → `USER_SELF_DEACTIVATION_FORBIDDEN`;
  unknown-id → `USER_NOT_FOUND`; tamper-role → `ADMIN_ROLE_CREATION_FORBIDDEN`;
  corrupt-state → `USER_ALREADY_DELETED` / `USER_NOT_DELETED`) emit ZERO
  audit rows — JR-C-1.**
```

The first bullet points developers to the canonical doc for the admin user-management surface. The second bullet restates the audit-emission rule inline (the most important contract that consumer tickets MUST follow). Both are rule-only — NO code, NO recipes (REQ-082 satisfied).

### 2. Root `AGENTS.md` — Important References section

Added one entry immediately after the existing `docs/teachers/applicant-lifecycle.md` entry (the precedent for this format):

```text
- `docs/admin/user-management.md` — Admin user-management canonical reference
  (DEV3-016: directory/filter/search contract incl. `escapeLikeWildcards`
  mandate, guarded soft-delete/reactivate pattern, role-child projection
  rules, audit-emission contract — writer-side, in-tx, denials write ZERO
  audit rows — JR-C-1, self-protection rule, `USER_NOT_FOUND` oracle ruling —
  admin-surface-only, MUST NOT be copy-pasted to non-admin surfaces, shared-PK
  "one user, four role children" model, idempotency ruling, scope-split record
  for DEV3-017..022b consumer obligations; A.5/A.7/B.6/B.7/INV-U1..U5/INV-TV1
  + Workflow 05 anchoring)
```

This entry registers the new canonical doc in the project-wide Important References registry so developers discover it during the AGENTS.md-discovery phase of any future task.

## 7.2.QL Quality Loop

Ran `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` on each touched `AGENTS.md` file:

| File | tsgo stage | oxlint stage | biome stage | lint:type-aware | check:duplicates | Exit |
|---|---|---|---|---|---|---|
| `docs/admin/user-management.md` | ✅ passed | ❌ "No files found to lint" (sandbox quirk — see note below) | n/a (oxlint hard-stop) | n/a | n/a | 1 |
| `backend/services/AGENTS.md` | ✅ passed | ❌ "No files found to lint" (sandbox quirk — see note below) | n/a | n/a | n/a | 1 |
| `AGENTS.md` (root) | ✅ passed | ❌ "No files found to lint" (sandbox quirk — see note below) | n/a | n/a | n/a | 1 |

### Oxlint "No files found to lint" — known sandbox quirk on `.md` files

Per DEV2-004's `outcome/7.1-outcome.md:46` (verbatim precedent, same behavior pattern):

> oxlint stage: ❌ tool exit 1 with `No files found to lint … Finished in 69ms on 0 files with 301 rules` — **NON-APPLICABLE for `.md`**: oxlint's configured scope contains zero markdown files; the exit-1 is the tool's hard-stop on an empty file set, not a finding. Proven file-independent: the identical failure occurs on untouched `.md` files.

This is a known sandbox quirk — markdown is outside oxlint's scope. The exit-1 hard-stop prevents the subsequent biome/lint:type-aware/check:duplicates stages from running, so the sub-loop's full lifecycle cannot complete for `.md` files. The compensating full-repo gates below confirm zero new errors.

### Compensating full-repo gates (the real quality gates for `.md`)

| Gate | Result |
|---|---|
| `bun tsgo` (project-wide) | exit **0**, 0 errors (matches Phase-0 baseline; matches all prior Phase 1-5 deltas). ZERO new type errors from the `AGENTS.md` pointer additions (rule-only text — no code, no TypeScript types touched). |
| `bun biome:check` | exit **0** — "Checked 540 files in 4s. No fixes applied. Found 8 warnings." — the 8 warnings are the pre-existing intentional `${...}` strings in `admin-user.repository.test.ts` security-scan oracle list (per Phase 0.1 baseline + 5.2-outcome.md). Biome ignores markdown (out-of-scope — markdown is not in biome's configured file types). |
| Schema/migration drift | `git diff --name-only backend/db/schema/ backend/db/migration/` → EMPTY. REQ-044 zero-drift gate preserved. |

## Files touched

- Modified: `backend/services/AGENTS.md` (added 2 rule-only bullet entries — pointer to `docs/admin/user-management.md` + inline audit-emission rule restatement).
- Modified: `AGENTS.md` (root — added 1 Important References entry under the existing `docs/teachers/applicant-lifecycle.md` entry).

No code files touched. No recipes. No implementation detail. REQ-082 satisfied.

## Sign-off

**Status:** ✅ PASS — REQ-082 satisfied. Both `AGENTS.md` files carry rule-only pointers to `docs/admin/user-management.md`. Sub-loop oxlint stage exhibits the known sandbox quirk on `.md` files (matches DEV2-004 7.1 precedent). Compensating full-repo gates GREEN: `bun tsgo` exit 0 / 0 errors, `bun biome:check` exit 0 / 8 pre-existing warnings. Zero schema/migration drift. Ready for Phase 7.3 (final completion gate + summary).
